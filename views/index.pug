extends layout

block content
  form(method="GET" action="/find" id="videoForm")
    input.urlInput(type='text' name="video_url" placeholder="Enter URL here..." id="videoUrl")
    input.searchBtn(type='submit' value="🔍")
  div.errorInfoPanel
    p.errorInfo
